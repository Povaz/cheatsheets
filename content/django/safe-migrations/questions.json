[
  {
    "question": "You add a required column with AddField(default='TBD'). Django's generated SQL adds the column with a database default and then immediately drops that default. Why does this break a rolling deploy?",
    "choices": [
      "Old code still running during the rollout inserts rows without naming the column; with the DB default gone, the NOT NULL column rejects those inserts — db_default persists the default and fixes it",
      "The DROP DEFAULT statement takes an exclusive table lock that blocks all reads for the duration of the rollout window",
      "New code reads the Python-side default while the database returns NULL, so the two code versions silently diverge on the value",
      "The migration becomes irreversible once the default is dropped, so a failed deploy strands the schema mid-change"
    ],
    "answer": 0,
    "explanation": "A rolling deploy has a coexistence window where old and new code hit the same schema, and the old code's INSERT statements simply don't mention the new column — only a database-level default can fill it in. Django drops the DB default because it manages defaults in Python at the ORM layer, which is safe on a single-version system but exactly wrong mid-rollout. db_default (Django 5.0+) persists the default in the schema itself, so the DROP DEFAULT never happens and old-code inserts keep succeeding. Lock contention is a real but separate migration concern, and the failure here is rejected writes, not divergent reads or irreversibility."
  },
  {
    "question": "You delete legacy_ref — a NOT NULL column with no default — from the model, and the RemoveField migration is correctly staged post-deploy. Yet during the rollout, as soon as new-code servers come up, their INSERTs start failing. Why?",
    "choices": [
      "RemoveField acquires an exclusive table lock that blocks concurrent inserts for the duration of the rollout window",
      "Django's runtime model state diverges from the schema, so the ORM raises as soon as it encounters a column it no longer knows about",
      "The new code's INSERTs no longer name legacy_ref, but the column still exists until the post-deploy drop — NOT NULL with no default rejects those writes; a pre-deploy step must first make the column nullable or give it a db_default",
      "The staging is inverted: destructive operations must run pre-deploy so that both code versions see the same schema throughout the rollout"
    ],
    "answer": 2,
    "explanation": "The remove side of the coexistence window is symmetric to the add side: the moment the field leaves the model, Django stops listing it in INSERT statements — and the column is deliberately still in the schema, because the drop is staged after the old code is gone. A NOT NULL column with no default rejects any insert that doesn't name it, so the new code breaks against a column only the old code still fills. Field removal is therefore inherently two-phase: a pre-deploy AlterField making the column nullable (or persisting a db_default), then the post-deploy RemoveField — django-syzygy generates that pre-deploy half automatically; with django-safemigrate you stage it yourself. No lock is involved since the drop hasn't run yet; the ORM never introspects the schema at runtime; and moving destructive drops pre-deploy would break the old code instead — the mirror-image failure."
  },
  {
    "question": "One release both adds Order.tracking_number (a nullable column the new code reads and writes) and deletes Order.legacy_ref — a nullable column the currently-deployed code still reads, which the new code drops entirely. The developer makes both model edits, runs makemigrations once, and gets a single migration containing the AddField and the RemoveField. The pipeline uses django-safemigrate, so a single Safe label must now cover the whole file. What does the three-stage model say about this migration?",
    "choices": [
      "It fits no slot, and no label can fix that. A migration's stage is a property of its operations, and this file mixes an additive one — AddField, pre-deploy by nature, harmless to old code — with a destructive one — RemoveField, post-deploy by nature, safe only once old code is gone. Label the file Safe.before_deploy() and the drop runs while still-deployed old code SELECTs legacy_ref: crash during rollout. Label it Safe.after_deploy() and the new code spends the rollout reading tracking_number before the column exists. The fix is upstream of labelling: split the model edits so each makemigrations run emits a single-stage migration — the AddField marked before_deploy, the RemoveField after_deploy. This is exactly the ambiguity syzygy's guardrail exists to catch: it raises a system check error on migrations mixing inferred stages, refusing to guess",
      "Mark it Safe.after_deploy(): when operations conflict, the destructive one sets the safety bar, so the whole file inherits the later slot — the AddField merely waits a little longer, which additive operations tolerate by definition",
      "Mark it Safe.before_deploy(): legacy_ref is already nullable, and nullable columns are safe to drop at any time — nothing at the database level can reject a write over a column that accepts NULL",
      "Keep one file but mark it Safe.after_deploy(delay=timedelta(days=7)): the delay guarantees every old-code server has drained before the migration applies, which makes the combined file safe in both directions"
    ],
    "answer": 0,
    "explanation": "The three-stage model's premise is that every migration sits on one side of the code swap, and the stage is determined by what its operations do to the coexistence window: additive changes leave old code's world intact (pre-deploy), destructive changes remove something old code may still touch (post-deploy). A file containing both is therefore not mislabelled but unlabelable — the two operations need to run on opposite sides of the same rollout, and a label applies to the file as a unit. Working both labels through the window proves it: before_deploy drops legacy_ref while the old version's SELECTs still name the column — every such query errors for the duration of the rollout; after_deploy withholds tracking_number while the new version's queries already reference it — same failure, other column, other direction. The guidance names this exact situation: a migration that fits none of the slots usually means several model edits were bundled into one run, and the cure is to split the edits so each generated file is single-stage. syzygy encodes the same rule as tooling — mixed inferred stages fail its system check, forcing an explicit decision. The distractors each generalize a half-truth: 'most conservative label wins' assumes waiting is free, but an additive migration the new code depends on is not optional during rollout — deferring it converts safety into breakage; 'nullable is droppable' confuses the two directions of the removal problem — nullability protects inserts from the new code, but the running old code still reads the column by name, and no DEFAULT or NULL saves a SELECT against a dropped column; and delay= only postpones when safemigrate applies an after_deploy migration — it stretches the wait, but the AddField trapped in the same file is still late by construction, so the rollout window is broken regardless of the timer's length."
  },
  {
    "question": "A team on django-safemigrate wires exactly one migration step into its pipeline: `python manage.py safemigrate` before the rollout, and nothing after — the reasoning being that a second plain `migrate` is redundant since safemigrate already applies everything it is allowed to. Several releases ship fine. Then a release whose only schema change is one nullable AddField fails at the safemigrate step. What is going on?",
    "choices": [
      "The earlier releases' `Safe.after_deploy()` migrations were never applied — safemigrate defers them to the post-deploy `migrate` that the pipeline never runs — so they accumulated unapplied; the new `before_deploy` migration depends on one of them, and safemigrate refuses rather than pull a post-deploy migration forward. The fix is to add the post-deploy `migrate` step, not to relabel anything",
      "A `delay=` timer set on an earlier `Safe.after_deploy()` migration has elapsed, and safemigrate errors once a delayed migration is overdue; bump the delay or apply that migration by hand",
      "Set `SAFEMIGRATE = \"nonstrict\"` so the run applies whatever is not blocked instead of erroring — the backlog then drains naturally over the following releases",
      "The new AddField was committed without a `safe` attribute, so it defaulted to `after_deploy`, safemigrate skipped it, and the resulting empty plan is what errors — add `Safe.before_deploy()` and the pre-commit `check` hook to prevent a recurrence"
    ],
    "answer": 0,
    "explanation": "safemigrate's two commands are two halves of one contract: `safemigrate` applies the pre-deploy (and `always`) migrations, and the ordinary `migrate` afterwards applies what was held back. Dropping the second call does not make post-deploy migrations run early — by design they are never applied by safemigrate, so every release quietly leaves its destructive migrations pending. Django's dependency graph then does the rest: the moment a new pre-deploy migration sits downstream of one of those unapplied post-deploy migrations, applying it would require applying the post-deploy one too, and safemigrate errors instead. The failure is therefore a pipeline defect surfacing as a guardrail, and the diagnosis matters more than the symptom — the broken release is merely the first one unlucky enough to depend on the backlog. The distractors are each a plausible misreading: `delay=` only postpones how soon `migrate` will apply an after_deploy migration and never turns into an error on expiry; `nonstrict` is a development convenience that skips blocked migrations rather than reporting them, which here would ship new code against a column that was silently never created — trading a loud failure for a silent one; and an unlabeled migration defaults to `Safe.always()`, not `after_deploy`, so it would have been applied without complaint (the pre-commit hook is still good practice, just not the cause)."
  },
  {
    "question": "A service already depends on a third-party package that installs its own `makemigrations` command (a migration-linearity checker that blocks merges creating parallel leaf migrations). The team now wants deploy-stage handling and is deciding between django-safemigrate and django-syzygy. One engineer frames the choice as 'whichever gives the stronger safety guarantee'. Why is that the wrong axis, and what actually decides it?",
    "choices": [
      "Both tools close the same coexistence-window gap, so safety is not the differentiator — integration surface is. syzygy works by replacing Django's `makemigrations` and `migrate`, so it collides head-on with the existing checker: keeping both means writing command classes that inherit from each. safemigrate adds a separate `safemigrate` command and leaves the built-ins untouched, so it composes with no glue — paid for with manual `safe` labels and hand-splitting the two-phase operations syzygy would have rewritten",
      "syzygy is the stronger of the two because it rewrites unsafe AddField and RemoveField automatically instead of trusting a human label, so the right move is to adopt syzygy and drop the linearity checker, whose guarantee syzygy's own stage inference subsumes",
      "Install both staging tools alongside the checker: safemigrate reads a `safe` attribute and syzygy reads a `stage` attribute, so they never contend for the same migration, and having two independent stage checks strictly reduces the chance of a mislabelled migration reaching production",
      "Neither tool can coexist with a third-party command override, so the only sound option is to drop both and script the phases by hand — `migrate <app> <target>` before the rollout for the additive migrations, then `migrate` after"
    ],
    "answer": 0,
    "explanation": "Both libraries answer the same question — which side of the code swap does this migration belong on — and both, used correctly, keep the rollout window safe; picking on 'safety' invents a distinction that isn't there. What genuinely differs is how much of Django each one takes over. syzygy overrides `makemigrations` and `migrate` because the framework offers no cleaner injection point, which is precisely why it does not stack with another package that overrides the same commands: preserving both guarantees requires your own command classes inheriting from both, permanent glue owned by your team. safemigrate ships a parallel `safemigrate` command and leaves `migrate` alone, so it drops in beside the checker with nothing to reconcile — but you now label every migration yourself and split the internally two-phase operations by hand, which for dropping a NOT NULL column costs an extra release. That is the real trade: manual labelling versus maintained integration glue. Dropping the linearity checker misses that its guarantee — a single migration leaf — is orthogonal to staging and not something stage inference provides. Installing both staging tools misreads where the conflict lives: distinct attribute names are irrelevant when the clash is over the command classes themselves, and two overlapping deploy gates give contradictory plans rather than defence in depth. And the hand-scripted path replaces a declarative, checkable property of each migration with pipeline commands that no system check or pre-commit hook can validate — strictly worse than either library."
  }
]
